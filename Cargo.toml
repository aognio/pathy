[package]
name = "pathy"
version = "0.1.0"
edition = "2024"
description = "A modern, visual CLI for exploring and editing PATH."
license = "MIT"

[dependencies]
clap = { version = "4", features = ["derive"] }
is-terminal = "0.1"
terminal_size = "0.4.4"
